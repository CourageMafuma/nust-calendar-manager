import 'package:flutter/material.dart';
import 'package:calendar_manager/main.dart'; // points to your actual app entry in lib/

void main() {
  runApp(
      const MyApp()); // or whatever your main widget is called in lib/main.dart
}
